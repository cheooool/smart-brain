import styled from 'styled-components';

const Form = styled.form`
  max-width: 340px;
  margin: 1em auto;
  padding: 2em 1em;
  box-shadow: 4px 4px 8px 4px rgba(0, 0, 0, 0.2);
`;

export default Form;
